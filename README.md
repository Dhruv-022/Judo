# Judo
Judo is a Discord bot focused on modular, database-backed server utilities, built with Python and discord.py cogs for clean separation of features. It uses JSON configuration and a dedicated database manager to persist server data, authorization lists, and dynamic settings, making it easy to maintain stateful features across restarts.
